

<p><%= image_tag @banner.pic.url(:medium) %></p>
<p>
  <strong>Name:</strong>
  <%= @banner.name %>
</p>

<b>Owner</b>
<%= @banner.user.profile_name %>

<p>


</p>

<p>
  <strong>Content:</strong>
  <%= @banner.content %>
</p>

 <% if user_signed_in? %>

<%= link_to 'Edit', edit_banner_path(@banner) %> |
<%= link_to 'Back', banners_path %>
<%= link_to "Delete", @banner, method: :delete, data: { confirm: "Are you sure your want to delete this banner?"} %>

<% end %>
